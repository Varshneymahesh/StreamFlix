

trigger:
- main

pool:
  vmImage: KDC1-L-F5596F8


stages:
  - stage: buildimage
    displayName: Build image
    jobs:
      - job: buildimage
        displayName: Build image
        steps:
        - task: DockerInstaller@0
          inputs:
            dockerVersion: '17.09.0-ce'
        - task: Docker@2
          inputs:
            containerRegistry: 'docker to azure'
            command: 'build'
            Dockerfile: '**/Dockerfile'